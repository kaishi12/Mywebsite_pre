@if (ViewBag.data != null)
{
    <span class="bookmarkb" id="delbookmarka" data-manga-id="@ViewBag.data.MangaId"><i class="fa fa-trash"></i> Xóa Khỏi Bookmark</span>
}
else
{
    <span class="bookmarkb" id="addbookmarkb"><i class="fa fa-heart"></i> Thêm Bookmark</span>
}
